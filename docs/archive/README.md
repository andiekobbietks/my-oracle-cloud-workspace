# Docs Archive

This folder is used as a target for relocated documents. It intentionally contains lightweight redirect/readme files so historical references remain discoverable.

If a document was moved, create a symlink or a short redirect file here that points to the new location.
